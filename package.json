{
  "name": "edm-midi-studio",
  "version": "0.1.0",
  "description": "Loopsmith — EDM MIDI generator with AI chat, audio preview, and Ableton-ready import/export",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check lib/theory.js && node --check lib/generators.js && node --check lib/midi.js",
    "samples": "node scripts/generate-samples.mjs",
    "test": "npm run check && npm run samples"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@tonejs/midi": "^2.0.28",
    "express": "^4.21.2"
  }
}
